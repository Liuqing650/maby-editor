import * as formatValue from './formatValue';
import { haveBlocks, haveInlines, haveMarks } from './have';
import removeMark from './removeMark';
import trailingSpace from './trailingSpace';

export {
  formatValue,
  trailingSpace,
  removeMark,
  haveMarks,
  haveBlocks,
  haveInlines,
};
